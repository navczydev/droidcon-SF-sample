<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DroidconSF"
        tools:targetApi="tiramisu">
        <!--
        If you don't provide a value, it defaults to false and does the following:
        - Disables the predictive back gesture system animation.
        - Ignores OnBackInvokedCallback, but OnBackPressedCallback calls continue to work.
        -->
        <activity
            android:name="com.navgde.droidconsf.activities.MainActivity"
            android:enableOnBackInvokedCallback="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.navgde.droidconsf.activities.SecondActivity"
            android:enableOnBackInvokedCallback="true"
            android:exported="false" />
    </application>

<!-- android:enableOnBackInvokedCallback="true" -->
</manifest>